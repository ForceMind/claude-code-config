# 本仓库说明

本仓库只存放 Claude Code 全局配置的源文件，不是代码项目。

| 仓库文件 | 安装位置 |
|---|---|
| `CLAUDE.global.md` | `~/.claude/CLAUDE.md` |
| `settings.json` | 合并进 `~/.claude/settings.json` |
| `statusline.py` | `~/.claude/statusline.py` |

全局提示词不叫 `CLAUDE.md`，是为了避免在本仓库工作时它同时作为全局指令和项目指令被加载两次。

## 修改后的维护纪律

1. 全局提示词的实质改动，先把改后全文交给用户复核，通过后再进入下面的步骤。
2. 改动上表任一文件后，运行 `./install.sh` 同步到 `~/.claude`（脚本先备份再覆盖）。在 `~/.claude` 里直接改过的，先复制回仓库再提交。
3. README 里的文件表和说明与实际保持一致。
4. 一次改动一个提交，提交信息说清改了什么、为什么。
5. 提交前检查 `settings.json` 没混入本机专属项（权限白名单、本机路径等）。
6. 提交后直接推送到 `origin main`。这是用户对本仓库的常设授权，仅限本仓库，不适用于其他项目。
